--- /tmp/gql-bundle-orig/src/Config/Parser/MetadataParser/MetadataParser.php	2026-03-11 12:34:25
+++ /Users/mathisfaivre/projects/graphqlbundle-issue-1205/vendor/overblog/graphql-bundle/src/Config/Parser/MetadataParser/MetadataParser.php	2026-03-11 12:33:46
@@ -151,7 +151,8 @@
                         $classMetadata,
                         $configs,
                         $gqlTypes,
-                        $preProcess
+                        $preProcess,
+                        $container
                     );
                 }
             }
@@ -172,7 +173,8 @@
         Meta $classMetadata,
         array $configs,
         array $gqlTypes,
-        bool $preProcess
+        bool $preProcess,
+        ContainerBuilder $container
     ): array {
         $gqlConfiguration = $gqlType = $gqlName = null;
 
@@ -259,6 +261,9 @@
             case $classMetadata instanceof Metadata\Provider:
                 if ($preProcess) {
                     self::$providers[] = ['reflectionClass' => $reflectionClass, 'metadata' => $classMetadata];
+                    if ($container->hasDefinition($reflectionClass->getName())) {
+                        $container->findDefinition($reflectionClass->getName())->setPublic(true);
+                    }
                 }
 
                 return [];
