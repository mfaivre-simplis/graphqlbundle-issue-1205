overblog_graphql:
    definitions:
        schema:
            query: RootQuery
        mappings:
            types:
                -   type: attribute
                    dir: '%kernel.project_dir%/src/UI/GraphQL'
                    suffix: ~
